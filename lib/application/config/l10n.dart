import 'package:flutter/material.dart';

class L10n {
  static final all = [
    const Locale('ar'), // Arabic
    const Locale('en'), // English
  ];

  static const langAr = Locale('ar');
  static const langEn = Locale('en');
}
